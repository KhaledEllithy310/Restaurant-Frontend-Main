import { TextMutedPipe } from './text-muted.pipe';

describe('TextMutedPipe', () => {
  it('create an instance', () => {
    const pipe = new TextMutedPipe();
    expect(pipe).toBeTruthy();
  });
});
